{
  "identifier": "default",
  "description": "Default capabilities for the audiobook generator",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "opener:default"
  ]
}
